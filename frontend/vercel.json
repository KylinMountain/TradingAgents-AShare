{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "git": {
    "ignoreBuildStep": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"release\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then exit 0; else exit 1; fi"
  },
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!assets/|vite.svg|favicon.ico).*)",
      "destination": "/index.html"
    }
  ]
}
